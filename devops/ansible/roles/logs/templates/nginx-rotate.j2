{{ nginx_log_dir }}/*.log {
  daily
  rotate 28
  missingok
  notifempty
  compress
  delaycompress
  dateext
  copytruncate
  sharedscripts
  postrotate
     /bin/kill -USR1 $(cat /var/run/nginx.pid 2>/dev/null || :
  endscript
}